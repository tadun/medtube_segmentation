# ── Virtual environments ──────────────────────────────────────────────────────
rs_env/
sort_env/
.venv/

# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.vscode/
pyrightconfig.json

# ── macOS ──────────────────────────────────────────────────────────────────────
.DS_Store

# ── Runtime caches ────────────────────────────────────────────────────────────
.matplotlib/
.ultralytics/

# ── Datasets (large binary files — download locally via Roboflow) ─────────────
dataset/
MedTube-2.yolov8/
balanced_yolo/
depth_yolo/
rgbd_split/

# ── Training run outputs (large) ─────────────────────────────────────────────
runs/
Ultralytics/

# ── Model weights — only the two final inference models are committed ─────────
*.pt
\!weights/yolo26n.pt
\!weights/yolo26n_depth-2.pt

# ── LaTeX build artefacts ─────────────────────────────────────────────────────
report/*.aux
report/*.bbl
report/*.bcf
report/*.bcf-SAVE-ERROR
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.out
report/*.pdf
report/*.run.xml

# ── Third-party SDKs ──────────────────────────────────────────────────────────
librealsense/

# ── Zip archives (dataset exports) ────────────────────────────────────────────
*.zip

# ── Archived / superseded data ────────────────────────────────────────────────
_archive/
