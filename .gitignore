# ── Virtual environments ──────────────────────────────────────────────────────
rs_env/
sort_env/
.venv/

# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# ── macOS ──────────────────────────────────────────────────────────────────────
.DS_Store

# ── Runtime caches ────────────────────────────────────────────────────────────
.matplotlib/
.ultralytics/

# ── Datasets (large binary files — download locally via Roboflow) ─────────────
dataset/
MedTube-2.yolov8/
balanced_yolo/
depth_yolo/
rgbd_split/

# ── Training run outputs (large) ─────────────────────────────────────────────
runs/
Ultralytics/

# ── Pretrained / downloaded weights (not project-specific) ───────────────────
*.pt
\!weights/*.pt
weights/rfdetr.pt
weights/yolov8m-seg.pt
weights/yolo11m-seg.pt
weights/yolo11n.pt
weights/yolo11n_weights.pt

# ── LaTeX build artefacts ─────────────────────────────────────────────────────
report/*.aux
report/*.bbl
report/*.bcf
report/*.bcf-SAVE-ERROR
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.out
report/*.pdf
report/*.run.xml

# ── Private development notes ─────────────────────────────────────────────────
docs/notes.md

# ── Third-party SDKs ──────────────────────────────────────────────────────────
librealsense/

# ── Archived / superseded data ────────────────────────────────────────────────
_archive/

# ── Zip archives (dataset exports) ──────────────────────────────────────────
*.zip
